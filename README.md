# ed-homework

Standalone React application scaffolded with Vite and configured for GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The GitHub Actions workflow `.github/workflows/github-pages.yml` runs three jobs: `validate`, `build`, and `deploy`.
